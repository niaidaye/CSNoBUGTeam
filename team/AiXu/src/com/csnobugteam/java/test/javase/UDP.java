package com.csnobugteam.java.test.javase;

/**
 * 编写一个UDP发送端，该发送端向接收端发送一条表示出生日期的字符串，格式是"yyyy-MM-dd"。
 * 接收端获取这个字符串以后，计算一下这个出生日期到现在的天数之差，并将这个天数之差返还给发送端（可以以字符串的形式返还）。
 * 发送端在接收到这个天数差后，打印一下这个天数差，打印的格式是："你已经出生了xxx天"
 * @author aixu
 * @since 2023/11/8
 */
public class UDP {
    public static void main(String[] args) {
        //
//        Date
    }
}
